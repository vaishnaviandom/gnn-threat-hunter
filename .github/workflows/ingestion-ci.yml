name: CI — Ingestion (Rust)

on:
  push:
    paths:
      - "ingestion/**"
  pull_request:
    paths:
      - "ingestion/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ingestion/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('ingestion/Cargo.lock') }}

      - name: Check (no compile errors)
        run: cargo check --manifest-path ingestion/Cargo.toml

      - name: Clippy (lint)
        run: cargo clippy --manifest-path ingestion/Cargo.toml -- -D warnings
